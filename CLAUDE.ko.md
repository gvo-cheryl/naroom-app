@AGENTS.md

# Naroom App 저장소 작업 지침

## 지침 구조

Claude Code는 다음 저장소 지침을 불러온다.

- `CLAUDE.md`: 앱 프로젝트 및 작업 규칙
- `AGENTS.md`: Expo, React Native 및 상세 프론트엔드 규칙
- `.claude/CLAUDE.md`: OMC 운영 규칙
- `docs/PRODUCT_CONTEXT.md`: 해당 파일이 존재할 경우 승인된 제품 맥락

이 파일은 `AGENTS.md`를 보완한다.
`AGENTS.md`에 있는 Expo 및 프론트엔드 규칙을 중복하거나 약화하지 않는다.

## 프로젝트 개요

- 프로젝트: `naroom-app`
- 역할: Naroom Expo React Native 모바일 앱
- 일반 개발 브랜치: `dev`
- 안정 브랜치: `main`
- 패키지 매니저: `pnpm`

Naroom은 기록, 회고, 장기 패턴, 사용자가 직접 선택하는 작은 변화를 통해 자신을 이해하도록 돕는다.

제품 동작이나 UI 구조를 변경하기 전에 다음을 확인한다.

1. 사용자의 현재 지시
2. 현재 GitHub Issue 또는 작업 요구사항
3. `AGENTS.md`
4. `docs/PRODUCT_CONTEXT.md`와 승인된 UI·UX 문서
5. 현재 화면 구조와 구현
6. 동기화된 OpenAPI 계약

자료가 실질적으로 충돌하면 제품을 임의로 다시 설계하지 않는다. 충돌을 보고하고 결정을 요청한다.

## 현재 기술 기준

- Node.js 24.18.0
- pnpm
- Expo SDK 57
- React Native
- Expo Router
- TypeScript
- mise 기반 프로젝트 런타임 버전 관리

실제 `package.json`, `pnpm-lock.yaml`, Expo 설정, TypeScript 설정, 기존 코드를 기준으로 판단한다.

패키지 매니저는 `pnpm`만 사용한다. npm이나 Yarn으로 변경하지 않는다.

현재 작업에 필요하지 않은 의존성, Expo 플러그인, 네이티브 모듈, 빌드 서비스, 분석 도구를 임의로 추가하거나 업그레이드하지 않는다.

## 주요 경로

현재 저장소 구조를 기준으로 판단한다.

일반적인 주요 경로는 다음과 같다.

- Route 및 화면: `app/`
- 공통 UI 컴포넌트: `components/`
- Hook: `hooks/`
- 공통 상수와 디자인 리소스: `constants/`
- 정적 리소스: `assets/`
- 제품 맥락: `docs/PRODUCT_CONTEXT.md`
- OpenAPI 스냅샷: `openapi/naroom-openapi.yaml`
- 생성 API 타입: 프로젝트 스크립트에서 정한 기존 생성 타입 경로
- Expo 설정: `app.json`, `app.config.*` 또는 현재 프로젝트의 대응 파일
- 패키지 스크립트와 의존성: `package.json`

같은 역할의 기존 경로가 있으면 별도 구조를 만들지 않는다.

## 로컬 실행

의존성 설치:

```bash
pnpm install
```

Expo 실행:

```bash
pnpm start
```

기존 package script에 정의된 대상을 실행한다.

```bash
pnpm ios
pnpm web
```

존재하지 않는 script를 임의로 가정하지 않는다. 먼저 `package.json`을 확인한다.

## 검증

`package.json`에 실제 존재하는 script를 사용한다.

기본 검증 명령은 다음과 같다.

```bash
pnpm lint
pnpm exec tsc --noEmit
pnpm exec expo-doctor
```

테스트 환경이 있으면 변경 동작에 대한 집중 테스트도 실행한다.

코드를 변경한 후 다음을 수행한다.

1. 변경한 컴포넌트, Route, Hook, API Adapter를 집중 검증한다.
2. lint를 실행한다.
3. TypeScript 타입 검사를 실행한다.
4. Expo Doctor를 실행한다.
5. 테스트가 존재하면 관련 테스트를 실행한다.
6. 런타임 확인이 필요하면 해당 플랫폼을 실행한다.
7. 최종 diff를 확인한다.
8. 변경 파일, 명령, 결과, 확인하지 못한 동작을 보고한다.

필수 검증이 실패했다면 완료됐다고 보고하지 않는다.

## 작업 범위 관리

수정 전에 다음을 수행한다.

1. 현재 Issue 또는 사용자 요구사항을 확인한다.
2. `AGENTS.md`와 관련 제품·UI 문서를 확인한다.
3. 구조 변경을 제안하기 전에 기존 구현을 확인한다.
4. 포함 범위와 제외 범위를 구분한다.
5. 수정 예정 파일과 검증 방법을 간단히 알린다.

요청이 명확하고 범위 안이라면 별도의 재승인을 기다릴 필요는 없다.

다음 상황에서는 중단하고 확인한다.

- 화면, Route, API 계약, 제품 문서가 실질적으로 충돌하는 경우
- 요청 범위에 없는 네이티브 설정이 필요한 경우
- 인증 또는 개인정보 정책이 결정되지 않은 경우
- 필요한 API 계약이 승인되지 않은 경우
- 기존 Navigation이나 상태 구조를 크게 다시 설계해야 하는 경우
- 비밀값, 서명 정보, 외부 계정 권한이 필요한 경우

관계없는 리팩터링, 이름 변경, 정리, 의존성 업그레이드, 재설계를 수행하지 않는다.

## UI 및 제품 구조 규칙

기존 프로토타입과 현재 구현을 UI 개선의 우선 기준으로 사용한다.

별도 요청이 없으면 다음을 수행하지 않는다.

- 프로토타입을 처음부터 다시 만들지 않는다.
- 전체 화면 구조를 다시 작성하지 않는다.
- 화면 ID를 일괄 변경하지 않는다.
- 필요 없이 기존 함수명이나 상태 객체명을 바꾸지 않는다.
- 화면을 삭제하지 않는다.
- Navigation을 재구성하지 않는다.
- 기존 흐름을 새로운 IA로 교체하지 않는다.
- 현재 제품 방향과 충돌하는 새 디자인 시스템을 도입하지 않는다.

문서와 프로토타입이 다르면 차이를 먼저 나열하고 승인된 최소 변경만 적용한다.

현재 제품 규칙은 다음과 같다.

- 기존 하단 Navigation의 기록 동작을 유지한다.
- 추후 전역 Floating 기록 버튼으로 변경하더라도 전체 재작성이 필요 없도록 컴포넌트를 구성한다.
- 공통 색상, 글꼴, 간격, 크기, 문구, 재사용 데이터를 중앙에서 관리한다.
- 승인된 라이트·다크 테마를 지원한다.
- 일반적인 사람, 요가, 명상 일러스트를 지양한다.
- 감정·에너지 강도는 내부적으로 0~100 값을 사용할 수 있지만 사용자를 평가하는 숫자로 표시하지 않는다.
- 승인된 한국어 사용자 문구를 사용한다.
- 승인된 대체 표현이 있다면 내부 용어 `AI`를 사용자 문구에 그대로 노출하지 않는다.
- 대체 문구가 확정되지 않았다면 임의로 만들지 않는다.
- 사용자 문구에는 `작은 실험`을 사용한다.
- 비교, 랭킹, 연속 기록 압박, 실패 표현, 강요되는 긍정을 피한다.
- 사용자가 해석에 동의하지 않거나 수정하거나 보류할 수 있게 한다.
- 필요한 화면에는 loading, empty, error, permission 상태를 포함한다.
- 한 화면에는 명확한 주요 행동 하나를 우선한다.

## 컴포넌트 및 TypeScript 규칙

- `AGENTS.md`의 Expo Router 규칙을 따른다.
- 애플리케이션 코드는 TypeScript를 사용한다.
- `any`보다 명시적인 타입을 우선한다.
- 이유 설명 없이 타입 오류를 억제하지 않는다.
- 화면 컴포넌트는 표시와 사용자 상호작용에 집중한다.
- API 접근, 데이터 변환, 상태 처리, 재사용 UI를 분리한다.
- 중복 컴포넌트를 만들기 전에 기존 컴포넌트를 재사용한다.
- 공통 Token, 문구, 설정, 상수를 중앙에서 관리한다.
- 한 번만 사용하는 구조를 성급하게 추상화하지 않는다.
- 플랫폼 전용 코드는 분리하고 명확하게 표시한다.
- 접근성 Label, 터치 영역, 명암 대비, 글자 크기, 키보드, Safe Area를 유지한다.
- 구현 편의를 위해 Route 이름이나 Navigation 동작을 변경하지 않는다.

## API 계약 규칙

앱은 공개 API 계약을 사용하며 백엔드 영속성 모델을 사용하지 않는다.

- JPA Entity나 DB 테이블에서 DTO를 추정하지 않는다.
- OpenAPI에서 이미 생성할 수 있는 타입을 수동으로 중복 작성하지 않는다.
- 생성된 API 타입을 수동으로 수정하지 않는다.
- 승인된 package script로 타입을 재생성한다.
- 앱 코드를 컴파일하기 위해 OpenAPI 스냅샷을 임의로 수정하지 않는다.
- 계약 문제는 백엔드 계약 담당 영역으로 보고한다.
- 안정적인 기계 판독용 API 오류 코드를 사용한다.
- 필수, 선택, nullable 필드를 구분한다.
- 필요한 경우 알 수 없는 Enum 값에 안전한 fallback을 제공한다.
- 승인된 ISO 8601 및 시간대 계약에 따라 날짜와 시간을 처리한다.
- 백엔드와 앱이 동시에 배포된다고 가정하지 않는다.
- 지원되는 백엔드 계약 및 앱 버전과의 호환성을 유지한다.

예정된 동기화 흐름은 다음과 같다.

```text
naroom-api에서 OpenAPI 생성
→ 승인된 OpenAPI 스냅샷
→ naroom-app으로 스냅샷 동기화
→ TypeScript 타입 생성
→ API Adapter
→ 화면 및 상태 연동
```

요청·응답 필드를 추정해서 앱 API 구현을 시작하지 않는다.

## 기록 및 AI 처리 동작

- 사용자의 원본 기록은 AI 처리와 독립적으로 먼저 저장한다.
- AI 처리 실패로 저장된 기록을 삭제하거나 덮어쓰지 않는다.
- 기록 저장 상태와 후속 회고 처리 상태를 명확히 구분한다.
- 후속 처리 실패는 복구 가능한 오류 상태로 제공한다.
- 생성된 해석을 확정적인 사실이나 진단처럼 표시하지 않는다.
- 홈, 위젯, 알림, 미리보기에 민감한 기록 원문을 불필요하게 노출하지 않는다.

## 인증 규칙

- Beta 1 소셜 로그인 범위는 카카오 로그인이다.
- 별도 요청 전에는 Google 로그인, 계정 연결, 계정 병합을 구현하지 않는다.
- 카카오 인증은 백엔드에 외부 사용자 신원을 전달하기 위해 사용한다.
- Naroom API 세션은 Naroom이 발급한 Access Token과 Refresh Token을 사용한다.
- 카카오 토큰을 Naroom 애플리케이션 세션 토큰으로 사용하지 않는다.
- Naroom 인증 정보는 승인된 보안 저장소에만 저장한다.
- Refresh Token을 AsyncStorage 또는 암호화되지 않은 일반 저장소에 보관하지 않는다.
- 인가 코드, 토큰, 비밀값, 전체 Authorization 헤더, 민감값을 포함한 카카오 응답을 로그에 남기지 않는다.
- 재발급, 회전, 로그아웃, 만료, 폐기는 승인된 백엔드 계약을 따른다.
- 무제한 재발급 또는 재시도 반복을 방지한다.
- 삭제 대기 계정을 자동 복구하지 않는다.
- 계정 접근을 복구하기 전에 승인된 탈퇴 철회 확인 흐름을 표시한다.

## 환경변수 및 비밀정보 규칙

Expo 클라이언트 코드와 `EXPO_PUBLIC_*` 값은 사용자에게 노출될 수 있으므로 비밀값을 포함하면 안 된다.

실제 비밀값은 다음에 작성하지 않는다.

- 소스 코드
- 테스트 fixture
- 로그
- `EXPO_PUBLIC_*` 환경변수
- `.env.example`
- Git에 포함되는 앱 설정
- 문서
- OpenAPI 스냅샷
- Git 커밋
- Claude 및 OMC 출력

Claude Code는 다음 내용을 읽거나 수정하거나 출력하지 않는다.

- `.env.local`
- `.env.*.local`
- 카카오 Client Secret
- 백엔드 비밀값
- OpenAI API 키
- 서비스 계정 JSON
- 서명 인증서
- 개인키
- keystore
- Provisioning Profile
- 실제 Access Token 및 Refresh Token
- 실제 사용자 기록

코드나 Git diff에서 비밀값을 발견하면 값을 반복하지 않는다. 해당 파일과 노출 가능성만 보고한다.

## 수정 금지 및 주의 파일

명시적 요청 없이 다음을 수정하지 않는다.

- `node_modules/`
- `.expo/`
- `.env.local`
- `.env.*.local`
- 네이티브 서명 파일
- 인증서와 keystore
- 생성된 API 타입의 수동 수정
- OpenAPI 스냅샷의 수동 수정
- 의존성 변경이 없을 때의 `pnpm-lock.yaml`
- 생성된 `ios/`, `android/` 네이티브 프로젝트
- Expo 애플리케이션 식별자
- Bundle ID와 Package Name
- EAS 프로젝트 및 빌드 설정
- `.gitignore`의 비밀정보 제외 규칙

의존성 변경이 승인되면 `pnpm`을 사용하고 변경된 lockfile을 함께 관리한다.

네이티브 또는 Expo 설정 변경이 필요하면 수정 전에 런타임 및 빌드 영향을 설명한다.

## OMC 및 저장소 경계

- OMC 운영 규칙은 `.claude/CLAUDE.md`를 따른다.
- OMC 실행 상태를 제품 기준 정보로 사용하지 않는다.
- 장기 보존할 결정은 승인된 프로젝트 문서에 남긴다.
- 앱 전용 작업에서 `naroom-api`를 수정하지 않는다.
- 승인된 API 계약을 독립적으로 변경하지 않는다.
- 해결되지 않은 동일 화면, 상태, 계약 결정을 병렬 Worker에게 중복 배정하지 않는다.
- 소유권과 작업 경계가 명확할 때만 병렬 작업한다.
- 앱 구현 중 계약 문제가 발견되면 스냅샷이나 생성 타입을 바꾸기 전에 작업을 중단하고 보고한다.

## Git 작업 규칙

- `dev`는 일반 개발 및 검증 브랜치다.
- `main`은 안정 브랜치다.
- 별도 요청 없이 `feature/*` 브랜치를 만들지 않는다.
- Pull Request는 필수가 아니며 요청된 대규모 또는 고위험 변경에서만 사용한다.
- 명시적 요청 없이 커밋, push, merge, rebase, PR 생성, Issue 종료, 원격 설정 변경을 수행하지 않는다.
- UI, API 연동, 생성 타입, 설정, 문서, AI 도구 변경은 가능한 한 논리적으로 분리한다.
- 로컬 환경파일, OMC 실행 상태, 개인 설정, 비밀값, 서명 파일, 사용자 데이터를 커밋하지 않는다.

## 작업 절차

1. `AGENTS.md`를 읽는다.
2. 관련 제품 및 UI 맥락을 확인한다.
3. 현재 Issue 또는 사용자 요구사항을 확인한다.
4. 기존 화면, 컴포넌트, Route, 상태, API 구조를 확인한다.
5. 포함·제외 범위를 정의한다.
6. 수정 예정 파일과 검증 방법을 알린다.
7. 승인된 범위만 구현한다.
8. lint, 타입 검사, Expo Doctor, 관련 테스트, 런타임 검증을 실행한다.
9. 최종 diff와 비밀정보 포함 여부를 검토한다.
10. 변경사항, 명령, 결과, 위험, 미확정 사항을 보고한다.
